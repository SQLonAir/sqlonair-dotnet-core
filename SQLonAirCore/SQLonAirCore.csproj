<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net5.0</TargetFramework>
    <Authors>SQL on Air, by Effortless API</Authors>
    <Company>An Abstract Level, LLC</Company>
    <Product>SQLonAir Core .NET Library</Product>
    <Description>This library can be used to define SQL on Air calculated fields natively in any .NET project.  

Extracts a SQL on Air fieldmap from a DLL notated with the SQLonAir .net Attributes defined in this package.  These include 
 - [SQLonAirLookup(...)], 
 - [SQLonAirComputation(...)], and 
 - [SQLonAirAggretation(...)].

The CSV can be generated by calling 
`SQLonAirManager.SaveCSV("CalculatedFields.csv", Assembly.GetExecutingAssembly());`

This can be called during startup - #if DEBUG.  That CSV can be used to generate the SQLonAir inject SQL script.</Description>
    <PackageId>SQLonAir.Core</PackageId>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <PackageProjectUrl>https://sqlonair.com</PackageProjectUrl>
    <PackageReleaseNotes />
    <AssemblyVersion>0.1.0.0</AssemblyVersion>
    <FileVersion>0.10.0.0</FileVersion>
    <Version>0.1.11</Version>
    <Copyright>Copyright 2021 (c) - SQLonAir.com</Copyright>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <RepositoryUrl>https://github.com/SQLonAir/sqlonair-dotnet-core</RepositoryUrl>
    <RepositoryType>Git</RepositoryType>
    <PackageTags>SQLonAir, Calculated Fields, Computed Columns, SQL Server Computed Columns</PackageTags>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Release|AnyCPU'">
    <DocumentationFile>SQLonAirCore\SQLonAirCore.xml</DocumentationFile>
  </PropertyGroup>

</Project>
